document.getElementById('form-1').addEventListener('submit', function (e) {
    e.preventDefault();
    // Handle message sending for Chat Window 1
    var message = document.getElementById('input-1').value;
    // Add logic to send message and display response
});

document.getElementById('form-2').addEventListener('submit', function (e) {
    e.preventDefault();
    // Handle message sending for Chat Window 2
    var message = document.getElementById('input-2').value;
    // Add logic to send message and display response
});
